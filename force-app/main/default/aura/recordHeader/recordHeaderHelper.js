({
	organizeData : function(cmp) {
        //const pickup_date = new Date(cmp.get("v.load.Pickup_By__c"));
        //const enUSFormatter = new Intl.DateTimeFormat('en-US');
        //cmp.set("v.pickupDate", enUSFormatter.format(pickup_date));
	}
})