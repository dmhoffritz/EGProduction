<apex:page sidebar="false" applyBodyTag="false" standardController="FreightTM__Load__c" extensions="RateConSignature" showheader="false" cache="true" docType="html-5.0" contentType="text/html" lightningStylesheets="true" standardStylesheets="false" >
    <apex:messages />
    <head>
        <meta http-equiv="Content-Type" content="text/html;charset=UTF-8" />
        <style type="text/CSS"> 
            @page { 
                size: 8.5in 11in; 
                margin: 1 auto;
                border: none;
                /* PDF width, height, margin */ 
            
            @top-center {
            content: element(header);
            }
            @bottom-left {
            content: element(footer);
            }
            }
            
            
            @media print { 
            body {zoom: 0.7; /* IE / -moz-transform: scale(0.7); / Firefox / -o-transform: scale(0.7); / Opera / -webkit-transform: scale(0.7); / Safari And Chrome / transform: scale(0.7); / Standard Property */}
            html, body {
                 border: none;
                 height: 100%;
                 page-break-after: avoid !important;
                 page-break-before: avoid !important;
                       } 
            .no-print, .no-print * { display: none !important; } 
            
            }
            
            body {
                font-family: Arial Unicode MS;
            }
            
            document.body.setAttribute('style', 'overflow: auto;');

            .blackline { border: 1px solid #000;}
            table { 
            border-collapse: collapse;
            border-spacing: 0;
            }
            
            th{
            border: 1px solid black;
            padding: 5px;
            font-size: .9em;
            font-weight: bold;
            text-align: center;
            }
            
            td{
            border: 1px solid black;
            padding: 5px;
            font-size: .9em;
            }
            
            .m-signature-pad--body-first canvas {
            position: relative;
            left: 0;
            top: 0;
            width: 75%;
            height: 100px;
            border: 1px solid #CCCCCC;
            border-radius: 10px 30px;
            }
            
            .m-signature-pad--body-third canvas {
            position: relative;
            left: 0;
            top: 0;
            width: 75%;
            height: 100px;
            border: 1px solid #CCCCCC;
            border-radius: 10px 30px;
            }
            
            .m-signature-pad--body-second canvas {
            position: relative;
            left: 0;
            top: 0;
            width: 75%;
            height: 100px; 
            border: 1px solid #CCCCCC;
            border-radius: 10px 30px;
            }
            .red {
              color: red;
            }
            .green {
              color: green;
            }
            .msgs {
              font-weight: bold;
              text-align: center;
              margin: 0 auto;
              width: 100%;
            }
            .button {
            background-color: #62BF34;
            border: none;
            color: #333;
            font-size: 1.3em;
            font-weight: 900;
            padding: 10px 15px;
            width: 100%;
            height: 25px;
            margin: 0 auto;
            letter-spacing: 1px;
            -webkit-appearance: none; <!-- to override iOS default btn style -->
            -moz-appearance: none; <!-- to override iOS default btn style -->
            }
            
        </style>
    </head>
    

  
    
</apex:page>